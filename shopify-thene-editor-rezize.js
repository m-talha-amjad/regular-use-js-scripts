$(document).on('themeEditorResize', function(event, mode) {
  if (mode === 'mobile') {
    // Do something when the customizer is in mobile view mode.
  } else if (mode === 'desktop') {
    // Do something when the customizer is in desktop view mode.
  } else if (mode === 'fullscreen') {
    // Do something when the customizer is in full-screen view mode.
  }
});
